# other-stuff
This is a repository for all the small projects I want to share but don't need their own repo.

## 1. vscode/vscodium code runner script
This is a small script + example settings.json to compile/run and debug programs. The current supported languages are:
1. C (gcc)
2. C++ (gcc)
3. Verilog (Icarus Verilog + GTKwave)
4. SystemVerilog (Icarus Verilog + GTKwave)
